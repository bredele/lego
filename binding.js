

/**
 * Expose 'Binding'
 */

module.exports = Binding;


/**
 * Binding constructor.
 * @api public
 */

function Binding(model) {
	if(!(this instanceof Binding)) return new Binding(model);
}


Binding.prototype.add = function() {
	// body...
};

Binding.prototype.apply = function() {
	// body...
};